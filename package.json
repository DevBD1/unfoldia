{
  "name": "partwise",
  "description": "Unfoldia — Explore the parts, understand the whole. Open-source interactive learning across subjects.",
  "author": "Burak (DevBD1)",
  "contributors": ["Unfoldia contributors"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DevBD1/partwise.git"
  },
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:docs": "node scripts/check-docs.js",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "check": "npm test && npm run build && node scripts/check-dist.js"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "license": "MIT",
  "dependencies": {
    "d3": "^7.9.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
